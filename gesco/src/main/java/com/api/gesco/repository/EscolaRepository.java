package com.api.gesco.repository;

import com.api.gesco.model.Escola;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EscolaRepository extends JpaRepository<Escola,Long> {
}
